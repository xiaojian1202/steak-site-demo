---
import {formatHoursLine, sortedByWeekStartingSunday, type DayHours} from '../lib/hours'

interface Props {
  shopName: string
  address?: {street?: string; city?: string; state?: string; zip?: string}
  phone?: string
  mapsUrl?: string
  hours?: DayHours[]
  holidayNote?: string
  social?: {platform: string; url: string}[]
}

const {shopName, address, phone, mapsUrl, hours, holidayNote, social} = Astro.props
const orderedHours = hours ? sortedByWeekStartingSunday(hours) : []
const addressLines = [
  address?.street,
  [address?.city, address?.state, address?.zip].filter(Boolean).join(', '),
].filter(Boolean)
---

<footer class="footer container">
  <div class="col">
    <h3>Hours</h3>
    <ul class="hours-list">
      {orderedHours.map((day) => <li>{formatHoursLine(day)}</li>)}
    </ul>
    {holidayNote && <p class="holiday-note">{holidayNote}</p>}
  </div>
  <div class="col">
    <h3>Visit</h3>
    {addressLines.map((line) => <p>{line}</p>)}
    {phone && <p><a href={`tel:${phone}`}>{phone}</a></p>}
    {mapsUrl && (
      <a class="button button--secondary" href={mapsUrl} target="_blank" rel="noopener">
        Directions
      </a>
    )}
  </div>
  {social && social.length > 0 && (
    <div class="col">
      <h3>Follow</h3>
      <ul class="social-list">
        {social.map((link) => (
          <li>
            <a href={link.url} target="_blank" rel="noopener">{link.platform}</a>
          </li>
        ))}
      </ul>
    </div>
  )}
  <p class="copyright">&copy; {new Date().getFullYear()} {shopName}</p>
</footer>

<style>
  .footer {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: var(--spacing-lg);
    padding: var(--spacing-lg) var(--spacing-sm) var(--spacing-md);
    border-top: 1px solid var(--color-border);
  }
  .hours-list,
  .social-list {
    list-style: none;
    margin: 0;
    padding: 0;
    color: var(--color-text-muted);
  }
  .holiday-note {
    font-style: italic;
    color: var(--color-text-muted);
  }
  .copyright {
    grid-column: 1 / -1;
    color: var(--color-text-muted);
    font-size: 0.85rem;
    margin: 0;
  }
</style>
